"use strict";

const name = "Claudia";

if (name == "Claudia") {
  console.log(`Bienvenida, ${name}`);
} else {
  console.log(
    "Lo siento pero el usuario que has introducido no está registrado"
  );
}

"use strict";

const text = document.querySelector(`.text`);
const almudena = "Almudena Blanco";

text.innerHTML = `El nombre de mi compañera es ${almudena}, y está compuesto por ${almudena.length} caracteres`;

"use strict";

const adalaber = {};
adalaber.name = "Susana";
adalaber.age = "34";
adalaber.job = "Periodista";
adalaber.run = function (a) {
  return `${a}`;
};

console.log(adalaber.run(`Estoy corriendo`));
